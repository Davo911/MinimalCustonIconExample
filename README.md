# CustomIconMinimalSetup
 Quasar-vue3 storybook minimal setup to demonstrate icon rendering Problem

# Before first run
`npm install`

# start quasar app
`quasar dev`

# start storybook
`npm run storybook`